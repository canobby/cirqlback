node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Local environment / secrets — never commit real keys.
# .env.example IS committed as the template.
.env
.env.*
!.env.example

# Personal credentials reference (plaintext logins) — never commit.
CREDENTIALS.md

# Playwright MCP scratch output (snapshots/screenshots from local testing)
.playwright-mcp/

# Replit agent local state / local tooling config (not part of the app)
.local/
.mcp.json

# Generated / local docs artifacts (keep local, don't commit)
Cirqlback-Linear-Checklist.pdf
NEXT.md